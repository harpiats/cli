import { Router } from "@harpia/core";
import { isAuthenticated } from "app/middlewares/auth";
import { controller } from "./controllers";

export const sessionRoutes = new Router("/session");

sessionRoutes.get("/", controller.show);
sessionRoutes.post("/", isAuthenticated, controller.store);
sessionRoutes.delete("/", isAuthenticated, controller.destroy);
