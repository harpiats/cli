import type { Request, Response, NextFunction } from "@harpiats/core";

export const {{ camelCase(name) }} = async (req: Request, res: Response, next: NextFunction) => {
  // Your middleware logic here

  next();
};
